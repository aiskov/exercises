package exercises;

public interface PalindromeString {
    /**
     * Check if this string palindrome. (You may check on WIKI)
     *
     * @param input Some string
     * @return true if string is palindrome (ignoring case and whitespaces on start and end) and false otherwise
     */
    boolean isPalindrome(String input);
}
